use std::fmt;

use mod(object)::*;

fun main() {
    let a = Object::from(1)
    in let b = a.add(2)
    in let i = 3
    in let c = a.add!(i)
    // error: in let d = a.add(4)

    in fmt::println("i = {}", i);
    fmt::println("b = {}, c = {}", b, c)
}
